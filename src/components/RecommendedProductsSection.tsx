// This component is no longer used — replaced by ProductStrip with autoplay.
// Kept as a placeholder; the "PRODUCTOS RECOMENDADOS" carousel is now
// rendered via <ProductStrip> in page.tsx.

export default function RecommendedProductsSection() {
  return null;
}
